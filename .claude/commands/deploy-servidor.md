---
name: deploy-servidor
description: Despliega y actualiza el programa en el servidor ejecutando el script
  de build. Úsalo cuando pidas: desplegar, actualizar servidor, subir cambios,
  publicar, hacer deploy o actualizar el programa en producción.
allowed-tools: Bash
---

# Deploy al servidor

Para actualizar el programa en el servidor, ejecuta este script:

```bash
cmd /c "C:\Users\calde\Desktop\upload_raspberry.cmd"